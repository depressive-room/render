#include "tagbody.h"
#include "taghtml.h"
#include "modulsystem.h"

namespace Render{

int main()
{
    ParserNode parserNode;
    QWidget* parent;
    ModulSystem.generate(parserNode.parserNode, parent);

    return 0;
}
}
